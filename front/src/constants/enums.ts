export enum LanType {
    lan = 'lan',
    wan = 'wan',
    none = 'none',
}

export enum ScreenSize {
    big = 'big',
    small = 'small',
}

export enum SwipeDir {
    none = 'none',
    left = 'left',
    right = 'right',
}

export enum DragType {
    DashboardWidget = 'DashboardWidget',
}
